package com.currenciesdirect.gtg.compliance.customcheck.authport;

//import com.currenciesdirect.gtg.es.compliance.auditlog.AuditLog;

//@Provider
//@PreMatching
public class ResponseInterceptor{/*implements ContainerResponseFilter {

	@Override
	public void filter(ContainerRequestContext requestCtx, ContainerResponseContext responseCtx) throws IOException {
		AuditLog auditLogMain = new AuditLog();
		int userID = Integer.parseInt((String) requestCtx.getProperty("
			request.setCorrelationID(headers.getRequestHeader("correlationID").get(0));
		"));
		auditLogMain.updateQuery(userID, responseCtx.getEntity().toString());
	}*/
}